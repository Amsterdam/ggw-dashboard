/* eslint-disable @typescript-eslint/no-unused-vars */
const DataTable = ({
  config,
  isCentered,
}: {
  config: string;
  isCentered: boolean;
}) => {
  return <div>DataTable</div>;
};

export default DataTable;
