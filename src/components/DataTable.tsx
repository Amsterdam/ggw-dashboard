import * as React from "react";

const DataTable = ({
  config,
  isCentered,
}: {
  config: string;
  isCentered: boolean;
}) => {
  return <div>DataTable</div>;
};

export default DataTable;
